/*
window.addEventListener('scroll', () => {
  const {
    scrollTop,
    scrollHeight,
    clientHeight
  } = document.documentElement;

  if (scrollTop + clientHeight >= scrollHeight - 800) //&& hasMoreQuotes(currentPage, limit, total)
  {
    //currentPage++;
    //loadQuotes(currentPage, limit);
    alert("scrolled!");
  }
}, {
  passive: true
});

*/
